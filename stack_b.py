"""Stack B module — has a syntax error."""


def calculate(x: int, y: int) -> int:
    """Calculate the sum."""
    return x + y


def broken(x)
    """Missing colon — syntax error."""
    return x * 2
