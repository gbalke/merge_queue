#!/usr/bin/env bash
set -euo pipefail

cd "$(git -C "$(dirname "$0")" rev-parse --show-toplevel)"

echo "Syntax check..."
find . -name '*.py' -not -path './.venv/*' -not -path './merge_queue.egg-info/*' | \
  xargs python -m py_compile

echo "Ruff lint..."
ruff check merge_queue/ tests/
